package com.pesapal.rdbms.storage;

/**
 * Supported data types for columns.
 */
public enum DataType {
    VARCHAR,
    INTEGER,
    BIGINT,
    DECIMAL,
    BOOLEAN,
    DATE,
    TIMESTAMP,
    TEXT
}
